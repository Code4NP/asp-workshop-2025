

docker build -t workshop .
docker run -it -p 8080:8080 workshop

docker run -it \
  -v "$(pwd)":/home/workshop \
  -p 8080:8080 -e PASSWORD="mysecretpassword" workshop \
    code-server --bind-addr 0.0.0.0:8080 /home/workshop
